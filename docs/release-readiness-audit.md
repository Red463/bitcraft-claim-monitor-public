# Release Readiness Audit

Status date: 2026-07-25

## Privacy release gates (2026-07-25)

The legal/privacy production gates were closed on 2026-07-25 with the temporary recovery exception recorded below:

- [x] Owner confirmed completion of the ICO data-protection fee self-assessment.
- [x] Owner confirmed solicitor review of the Terms, Privacy Policy, both legitimate-interest assessments, England/Wales wording, international transfers, 18+ approach, and the absence of a published postal address.
- [x] Owner confirmed HostWorld volume and provider-snapshot encryption at rest.
- [x] Provisioned `/etc/bitcraft-claim-monitor/backup-encryption.key` as a validated 32-byte base64url value, root-owned mode `0600`.
- [x] Provisioned `/etc/bitcraft-claim-monitor/privacy-ledger.key` as a separate validated 32-byte base64url value, root-owned, group `bitcraft`, mode `0640`.
- [x] Recorded the owner's temporary acceptance of regular full-VPS backups as the deletion-ledger recovery copy. This is not independent of the VPS or HostWorld failure domain; the approved limitation and Proton Drive follow-up are in `docs/superpowers/specs/2026-07-25-temporary-vps-ledger-recovery-design.md`.
- [x] Ran a protected Linux SQLite backup, AES-256-GCM encrypt/decrypt round trip, two `PRAGMA quick_check` validations, signed-ledger replay, and plaintext temporary-file cleanup.
- [x] Owner confirmed the Discord Developer Portal Terms and Privacy URLs are updated.
- [x] Owner confirmed Namecheap, Proton, GitHub, Buy Me a Coffee, Discord, BitJita, and HostWorld disclosures against the live arrangement.
- [x] Inventoried legacy plaintext backups; converted the six retained recovery points to validated `.sqlite.enc` files, guarded-pruned 22 older predeploy copies, and removed the checksum-validated obsolete plaintext database/environment bundle.

Off-VPS hardening remains scheduled: copy uniquely named signed-ledger snapshots to `Proton Drive/My files/Timbersteel Claim Monitor/Privacy Recovery/Deletion Ledger/`, verify a restore, and then remove the temporary same-VPS exception.

This audit tracks public-release readiness for the maintained app at `apps/bitcraft-local`. It records evidence from the current codebase and the remaining work that must be verified before the release-readiness goal can be marked complete.

## Current Handoff Summary

The release-readiness pass has completed the major maintainability, server-helper, stylesheet-ownership, notification-architecture, security/config, documentation, build, and automated test work that can be proven from the local worktree. `src/pages/MainPages.tsx` has been removed, `AppShell` is back to top-level orchestration, many dependency-light `server.mjs` policies now live in tested helper modules, notification UI/source/timer logic is split under `src/notifications/`, and focused stylesheet modules now own routed pages, admin, bot, app chrome, user settings, and notification UI.

Current automated gate evidence is fresh for this worktree: production build/typecheck passes and the full app test suite passes with 250/250 tests. There is no configured lint script; the app build is the current TypeScript gate.

The release-readiness goal is still not complete because three live-source notification checks need real environment evidence before the notification requirement can be honestly closed: `market-deal-alert`, `production-started`, and `production-completed`. Loopback smoke evidence proves those notification types render globally across routed main-app pages, but the remaining checks must prove real signed-in deal-alert rows and real production queue diffs enter the same global notification path.

## Current Structure Findings

| Area | Current state | Release decision |
| --- | --- | --- |
| `src/main.tsx` | Small bootstrap file that imports styles and renders `AppShell`. | Keep as-is. It is already the right boundary. |
| `src/AppShell.tsx` | About 569 lines. Owns app chrome, global refresh, route selection, auth state, analytics consent, notification hook orchestration, and drawer route actions. Admin console rendering, bot dashboard sections, loopback-only smoke wiring, live notification source baselines, toast/log/timer state, and browser-local settings UI now live in focused modules, and stale admin/page imports have been removed. | Keep as the top-level browser shell. Avoid adding page, admin-console, bot-section, or dialog internals here. |
| `src/components/admin/AdminPanel.tsx` | About 1,943 lines. Owns the admin console and dedicated `/bot` management UI, including admin auth state, settings drafts, scheduled jobs, diagnostics, database inspection, account/user management, analytics/security panels, and Discord bot tools. Pure display labels and formatters now live in `src/components/admin/adminDisplay.ts`. | Still large, but it is isolated from the public app shell and pure display helpers. Future admin work can split stable tab panels or admin hooks from this module with focused tests. |
| `src/components/admin/adminDisplay.ts` | About 132 lines. Owns pure admin display helpers for byte labels, collector purpose/status text, scheduled-job progress text, and Discord audit labels/change summaries. | Keep pure admin label/formatting logic here so it can be tested without rendering the admin console. |
| `src/components/main/UserSettingsDialog.tsx` | About 457 lines. Owns the browser-local settings dialog, including account linking controls, local density/notification preferences, theme presets/editor/import-export, sound preview settings, and local reset UI. | Keep browser-local settings UI here; AppShell should pass state/actions and avoid reabsorbing dialog markup. |
| `src/pages/DashboardPage.tsx` | About 235 lines. Owns the routed Dashboard page component, including live summary cards, historical snapshot trends, and Dashboard-specific page composition. | Keep Dashboard out of the legacy page bundle; move any future Dashboard-only helpers beside this page. |
| `src/pages/InventoryPage.tsx` | About 242 lines. Owns the routed Inventory page component, including storage filters, core material summary cards, item detail lookup state, and container table composition. | Keep Inventory page-owned state and material summary rendering here; shared item display primitives stay in `src/components/main/ItemDisplay.tsx`. |
| `src/pages/MapPage.tsx` | About 254 lines. Owns the routed Map page component, including player/resource/region filters, local map catalog loading, BitCraft map URL generation, and map URL diagnostics. | Keep Map page-owned state and map catalog rendering here; keep reusable URL/resource helpers in `src/pages/map/mapUtils.ts`. |
| `src/pages/PublicCraftFinderPage.tsx` | About 167 lines. Owns the routed Public Craft Finder component, including skill/region filters, public craft fetch state, sorting, summary cards, and map-focus handoff. | Keep Public Craft Finder page-owned state and sorting here; keep shared active-region fetch/label helpers in `src/hooks/useActiveRegions.ts`. |
| `src/pages/ProductionPage.tsx` | About 305 lines. Owns the routed Production page component, including craft sorting, member eligibility checks, toolbelt lookup state, private-craft visibility, and passive-craft history rendering. | Keep Production page-owned state and passive-craft rendering here; shared production metric/status helpers stay in `src/pages/production/productionUtils.ts`. |
| `src/pages/MarketPage.tsx` | About 411 lines. Owns the routed Market page component, including live listings, analytics, market tabs, and market-owned browser state. | Keep as the routed Market entrypoint. Future market work can split tab panels when behavior changes justify it. |
| `src/pages/market/PriceFinder.tsx` | About 328 lines. Owns the price lookup and deal-watch subtool, including item search, price-history fetch state, regional price filters, deal-watch settings, and recent-trade rendering. | Keep price/deal-watch-specific state and rendering here; shared active-region, navigation, analytics, item, and formatting helpers stay imported from shared modules. |
| `src/pages/market/BuyOrderFinder.tsx` | About 206 lines. Owns the buy-order finder subtool, including cached buy-order fetch state, region/page/sort controls, opportunity cards, and the buy-order table. | Keep buy-order-specific state and rendering here; shared active-region, navigation, analytics, and formatting helpers stay imported from shared modules. |
| `src/pages/MainPages.tsx` | Removed after the remaining Market code moved into `src/pages/MarketPage.tsx`. | Do not recreate this legacy mixed page bundle. New page code should live in feature-owned page modules. |
| `server.mjs` | Single large Node service after extracting route classification, dependency-free HTTP policy helpers, response writers, cookie helpers, request-origin helpers, CSRF helpers, body parsing helpers, rate-limit helpers, visitor IP privacy helpers, visitor-security settings helpers, notification-activity redaction helpers, market deal-alert formatting helpers, market deal-watch settings helpers, market activity normalization helpers, production activity normalization helpers, recipe catalog normalization helpers, Discord settings normalization helpers, collector settings normalization helpers, scheduled-job schedule/bootstrap/recovery/status helpers, BitJita proxy-cache helpers, admin permission helpers, public user payload helpers, admin request guard helpers, auth identity helpers, session policy helpers, session lookup helpers, Discord OAuth config helpers, OAuth flow helpers, OAuth state helpers, schema bootstrap SQL helpers, prepared-statement factory helpers, default owner admin bootstrap helpers, schema migration metadata/application/cleanup helpers, default app setting bootstrap/application helpers, release metadata helpers, app settings policy helpers, password-auth helpers, and admin login-attempt helpers. Owns static serving, BitJita proxy route wiring, SQLite schema/migrations, prepared statements, local/admin APIs, app/admin auth, Discord, analytics, collectors, backups, and scheduled jobs. | Needs further deliberate server-module splits. The first safe extractions are `src/server/httpRoutes.mjs`, `src/server/httpResponses.mjs`, `src/server/httpCookies.mjs`, `src/server/httpRequests.mjs`, `src/server/httpCsrf.mjs`, `src/server/httpBodies.mjs`, `src/server/httpRateLimit.mjs`, `src/server/visitorIp.mjs`, `src/server/visitorSecuritySettings.mjs`, `src/server/notificationActivity.mjs`, `src/server/dealAlerts.mjs`, `src/server/marketDealWatchSettings.mjs`, `src/server/marketActivity.mjs`, `src/server/productionActivity.mjs`, `src/server/recipeCatalog.mjs`, `src/server/discordSettings.mjs`, `src/server/collectorSettings.mjs`, `src/server/scheduledJobs.mjs`, `src/server/bitjitaProxyCache.mjs`, `src/server/adminPermissions.mjs`, `src/server/publicUsers.mjs`, `src/server/adminRequestGuards.mjs`, `src/server/authIdentity.mjs`, `src/server/serverSessions.mjs`, `src/server/sessionLookups.mjs`, `src/server/discordOAuthConfig.mjs`, `src/server/discordOAuthFlow.mjs`, `src/server/oauthState.mjs`, `src/server/schemaBootstrap.mjs`, `src/server/preparedStatements.mjs`, `src/server/defaultOwnerAdmin.mjs`, `src/server/schemaMigrations.mjs`, `src/server/defaultAppSettings.mjs`, `src/server/appRelease.mjs`, `src/server/appSettingsPolicy.mjs`, `src/server/passwordAuth.mjs`, and `src/server/adminLoginAttempts.mjs`; route/auth/persistence boundaries must remain under test before moving larger code. |
| `src/styles.css` | Large global stylesheet, now 1470 lines, containing tokens, layout, navigation, tables, shared item/tier primitives, page-specific sections not yet extracted, broad responsive rules, and browser theme propagation. Notification, dashboard, leaderboard, production, public craft finder, market, craft calculator, members, skills, inventory, construction, research, activity, region, sync, map, admin console, Discord/bot admin sections, bot dashboard shell, app-chrome, user-settings, and Empires UI styles have been extracted. | Needs continued CSS audit and incremental extraction. Avoid broad formatting churn; split only stable sections with clear ownership. |
| `src/styles/setup-workflow.css` | Small focused module for setup checklist, admin message state, and bot workflow polish. | Keep as a descriptive setup/workflow stylesheet and use it as the replacement for the old phase-numbered module naming pattern. |
| `src/styles/dashboard.css` | Focused module for routed Dashboard page shell, KPI metrics, dashboard cards, feed/member/production/alert lists, legacy dashboard top-meta chrome, and Dashboard-specific responsive rules. | Keep Dashboard route styling out of the global stylesheet; the legacy top-meta chrome remains globally imported through this module until those shared page-header classes are renamed or split. |
| `src/styles/leaderboard.css` | Focused module for routed Leaderboard page cards, tabs, summary metrics, ranking lists, and responsive leaderboard behavior. | Keep Leaderboard page styling out of the global stylesheet; the existing tab base class remains globally imported because Empires intentionally reuses it with an `empires-tabs` modifier. |
| `src/styles/production.css` | Focused module for routed Production page cards, craft grids, crafter pills, member banners, private-craft controls, crafter filter row, passive-craft table treatment, and responsive Production behavior. | Keep Production page styling out of the global stylesheet; shared `command-filter-*` and browser theme-propagation selectors stay global because multiple routed pages reuse those shared patterns. |
| `src/styles/public-craft.css` | Focused module for routed Public Craft Finder page shell, summary cards, filter hint, table treatment, and page-specific controls. | Keep Public Craft Finder page styling out of the global stylesheet; browser theme-propagation selectors stay global because other pages reuse those patterns; Public Craft map links now stay page-owned. |
| `src/styles/inventory.css` | Focused module for routed Inventory page shell, summary cards, material watch/detail panels, filters, toggles, container cards, and page-specific table treatment. | Keep Inventory page styling out of the global stylesheet; shared container/material primitives and browser theme-propagation selectors stay global because other pages reuse those patterns. |
| `src/styles/construction.css` | Focused module for routed Construction page shell, summary cards, warning section, material needs, project cards, progress rows, completion controls, and page-specific responsive rules. | Keep Construction page styling out of the global stylesheet; shared project/material primitives and browser theme-propagation selectors stay global because other pages reuse those patterns. |
| `src/styles/research.css` | Focused module for routed Research page shell, summary cards, unlock chips, filter panel, research lanes, and technology cards. | Keep Research page styling out of the global stylesheet; shared `research-filter-field` controls, broad responsive grid collapse, and browser theme-propagation selectors stay global because other pages reuse those patterns. |
| `src/styles/activity.css` | Focused module for routed Activity page shell, summary cards, filter panel, category controls, timeline, event rows, and search-loading state. | Keep Activity page styling out of the global stylesheet; shared focus rules, broad responsive grid collapse, and browser theme-propagation selectors stay global because other pages reuse those patterns. |
| `src/styles/region.css` | Focused module for routed Region page shell, rank and summary cards, regional insights, supply leaders, nearby settlement panel, and regional rankings table treatment. | Keep Region page styling out of the global stylesheet; shared table/sort-button primitives, broad responsive grid collapse, and browser theme-propagation selectors stay global because other pages reuse those patterns. |
| `src/styles/sync.css` | Focused module for routed Sync page shell, topbar, and embedded BitCraft Sync iframe treatment. | Keep Sync page styling out of the global stylesheet; shared topbar/meta/button primitives stay global because other pages reuse those patterns. |
| `src/styles/market.css` | Focused module for routed Market page shell, live and analytics grids, command/filter panels, best-seller leaderboard, price finder, buy-order finder, deal-watch controls, pagination, and Market-specific responsive layout rules. | Keep Market page styling out of the global stylesheet; shared `command-filter-header`, `suggestion-*`, table, and theme-propagation selectors stay global because other routed pages or browser theme rules still reuse them. |
| `src/styles/craftcalc.css` | Focused module for routed Craft Calculator page shell, lookup controls, recipe-route cards, summary cards, material rows, step cards, warning state, and empty state. | Keep Craft Calculator page styling out of the global stylesheet; shared `command-filter-header`, `suggestion-*`, and `research-filter-field` primitives stay global because other routed pages reuse them. |
| `src/styles/members.css` | Focused module for the routed Members page shell, roster summary, member table treatment, public-profile equipment panels, gear presets, passive-craft list, and responsive Members behavior. | Keep Members page styling out of the global stylesheet; shared item thumbnail/label and item-detail primitives stay global because Inventory and shared item display components reuse them. |
| `src/styles/skills.css` | Focused module for the routed Professions/Skills page shell, focus and coverage panels, adventure skill cards, heatmap table treatment, tier legend, and responsive Skills behavior. | Keep Skills page styling out of the global stylesheet; shared summary-grid, mini-stat, tier badge, and base legend primitives stay global because other pages reuse them. |
| `src/styles/map.css` | Focused module for routed Map page shell, player/resource selectors, focus banner, map iframe, and Map-specific responsive layout rules. | Keep routed Map page styling out of the global stylesheet; admin-only map URL diagnostics and shared touch-target rules stay global because they render outside the Map page or apply across controls. |
| `src/styles/admin.css` | Focused module for shared admin console styles, admin login/loading state, admin metrics, scheduled jobs, collector settings, database inspection, linked accounts, and admin-specific responsive rules. | Keep shared admin console styles out of the global stylesheet. |
| `src/styles/discord-admin.css` | Focused module for Discord and bot admin section layouts, role-panel editors, colour-role controls, moderation panels, diagnostics, test controls, terminal/log previews, and their responsive rules. | Keep Discord-specific admin section styles out of the global stylesheet and separate from the `/bot` shell/navigation stylesheet. |
| `src/styles/bot-dashboard.css` | Focused module for the dedicated `/bot` dashboard shell, overview metrics, section navigation, and responsive shell behavior. | Keep bot shell/navigation styles out of the global stylesheet. |
| `src/styles/app-chrome.css` | Focused module for floating app tools, shared help/legal dialogs, Discord sign-in dialog, cookie consent, command palette shell, and their mobile overrides. | Keep shared app-chrome overlays out of the global stylesheet; preserve the import order after global tokens and before notification overlays. |
| `src/styles/user-settings.css` | Focused module for the browser settings dialog, account-linking cards, theme editor, and settings-specific responsive rules. | Keep feature-owned settings styles out of the global stylesheet. |
| `src/styles/notifications.css` | Focused module for toast stack, notification drawer, notification badge, notification sound settings, and responsive notification overrides. | Keep as the pattern for feature-owned stylesheet modules with clear ownership. |
| `src/styles/empires.css` | Focused module for routed Empires page controls, status cards, watchtower scanning tables, coordinate controls, and responsive Empires layouts. | Keep as a page-owned stylesheet while the related page remains routed and behavior-specific. |
| Notifications | Rendering, generation, source queueing, smoke helpers, smoke/live-source hook wiring, and toast/log state have been split into `src/components/main/Notifications.tsx` and `src/notifications/`. | Architecture is improved and the sample browser/page matrix has evidence; live-source production queue and signed-in deal-alert verification are still required. |

## Completed Release-Readiness Improvements

- Extracted pure notification creation, dedupe, source drafting, unseen selection, market activity source, signed-in deal-alert source, production queue diff, toast-stack, persisted-log, drawer read-state helpers, and mounted toast/log/timer state.
- Extracted activity summary, diff, toast, and activity-log cleanup helpers into `src/pages/activity/activityUtils.ts`.
- Extracted the routed Dashboard page into `src/pages/DashboardPage.tsx` and shared activity display labels/icons into `src/pages/activity/activityDisplay.tsx`, reducing the legacy `MainPages.tsx` bundle.
- Extracted the routed Leaderboard page into `src/pages/LeaderboardPage.tsx`, keeping its tabs, sorting state, local fetch, and member comparison transforms beside the page.
- Extracted the routed Activity page into `src/pages/ActivityPage.tsx`, keeping filters, search, compacting, and timeline rendering beside the activity-owned helper modules.
- Extracted the routed Inventory page into `src/pages/InventoryPage.tsx`, keeping storage filters, core material summaries, item detail lookup state, and container table composition beside the page.
- Extracted the routed Map page into `src/pages/MapPage.tsx`, keeping player/resource/region map filters, local catalog loading, embedded map URL state, and map diagnostics beside the map-owned helper module.
- Extracted the routed Public Craft Finder into `src/pages/PublicCraftFinderPage.tsx`, keeping skill/region filters, public craft fetch state, sorting, summary cards, and map-focus handoff beside the page.
- Extracted the routed Production page into `src/pages/ProductionPage.tsx`, keeping craft sorting, member eligibility checks, toolbelt lookup state, private-craft visibility, and passive-craft history rendering beside the page.
- Extracted the routed Market page into `src/pages/MarketPage.tsx`, removing the legacy `src/pages/MainPages.tsx` bundle while keeping market-specific tools together.
- Extracted the Market price finder into `src/pages/market/PriceFinder.tsx` and buy-order finder into `src/pages/market/BuyOrderFinder.tsx`, reducing the routed Market entrypoint while keeping market subtools beside other market-owned helpers.
- Extracted browser-local settings UI into `src/components/main/UserSettingsDialog.tsx`, keeping AppShell focused on opening the dialog and wiring global settings/auth actions.
- Extracted admin console rendering into `src/components/admin/AdminPanel.tsx`, keeping AppShell focused on public route orchestration while preserving the same admin and `/bot` wiring.
- Removed stale admin, bot, page-helper, and settings-dialog imports from `src/AppShell.tsx` after the extractions.
- Extracted pure admin display helpers into `src/components/admin/adminDisplay.ts`, covering byte labels, collector status/purpose text, scheduled-job progress text, and Discord audit labels.
- Extracted production craft activity-window, progress-key, and metrics helpers into `src/pages/production/productionUtils.ts`.
- Extracted map URL and resource-token helpers into `src/pages/map/mapUtils.ts`.
- Extracted market analytics transforms and best-seller sort helpers into `src/pages/market/marketAnalytics.ts`, plus market listing display, age, and tracking helpers into `src/pages/market/listingUtils.ts`.
- Extracted member identity helpers into `src/utils/memberIdentity.ts`.
- Extracted shared active-region fetch and label helpers into `src/hooks/useActiveRegions.ts` so Market, Buy Order Finder, Public Craft Finder, and Map no longer depend on helper definitions inside the legacy page bundle.
- Added focused tests for notification, activity, map, member identity, market analytics, market listing helper behavior, the Market page shared browser-helper boundary, the Market page boundary, the Price Finder boundary, the Buy Order Finder boundary, the Dashboard page boundary, the Leaderboard page boundary, the Activity page boundary, the Inventory page boundary, the Map page boundary, the Public Craft Finder page boundary, the Production page boundary, and the active-region helper boundary.
- Added notification architecture documentation in `docs/notification-system.md`.
- Added this audit and the developer guide to make the current architecture and remaining release blockers explicit.
- Replaced the generic `.env.example` with app-specific environment variables and secret placeholders.
- Fixed the `admin` role permission set so non-owner administrators can access admin-user management routes that require `users.manage`, while viewer-level users remain denied.
- Extracted notification UI styles into `src/styles/notifications.css` so toast, drawer, badge, sound-setting, and responsive notification rules have clear ownership.
- Extracted user settings and theme-editor UI styles into `src/styles/user-settings.css` so settings-specific selectors are no longer mixed into the global stylesheet.
- Extracted shared admin console, loading, scheduled-job, collector, brand-upload, database-inspection, linked-account, admin-user, audit, backup, maintenance, and admin responsive styles into `src/styles/admin.css`.
- Extracted Discord and bot admin section layouts, role-panel editors, colour-role controls, moderation panels, diagnostics, tests, and terminal/log preview styles into `src/styles/discord-admin.css`.
- Moved Market deal-watch action, watchlist, threshold, and empty-state styles into `src/styles/market.css`.
- Renamed `src/styles/phase6.css` to `src/styles/setup-workflow.css` so setup checklist and bot workflow styles have a descriptive owner instead of phase-number naming.
- Extracted shared app-chrome styles into `src/styles/app-chrome.css` so floating tools, shared dialogs, cookie consent, and command palette shell styles have a named owner outside the global stylesheet.
- Extracted routed Empires page styles into `src/styles/empires.css` so watchtower scouting controls and Empires-specific responsive rules have a page-owned stylesheet.
- Extracted routed Leaderboard page styles into `src/styles/leaderboard.css` so ranking tabs, summary cards, profession lists, recent activity lists, and responsive leaderboard rules have a page-owned stylesheet.
- Extracted routed Production page styles into `src/styles/production.css` so production cards, craft grids, crafter pills, private-craft controls, crafter filter rows, passive-craft table treatment, and responsive production rules have a page-owned stylesheet.
- Extracted routed Public Craft Finder styles into `src/styles/public-craft.css` so the page shell, summary cards, filter hint, table treatment, and page-specific controls have a page-owned stylesheet.
- Extracted routed Inventory styles into `src/styles/inventory.css` so the page shell, summary cards, material watch/detail panels, filters, toggles, container cards, and page-specific table treatment have a page-owned stylesheet.
- Extracted routed Construction styles into `src/styles/construction.css` so the page shell, summary cards, warning section, material needs, project cards, progress rows, completion controls, and page-specific responsive rules have a page-owned stylesheet.
- Extracted routed Research styles into `src/styles/research.css` so the page shell, summary cards, unlock chips, filter panel, research lanes, and technology cards have a page-owned stylesheet while shared filter controls stay global.
- Extracted routed Activity styles into `src/styles/activity.css` so the page shell, summary cards, filter panel, category controls, timeline, event rows, and search-loading state have a page-owned stylesheet.
- Extracted routed Region styles into `src/styles/region.css` so the page shell, rank and summary cards, regional insights, supply leaders, nearby settlement panel, and regional rankings table treatment have a page-owned stylesheet.
- Extracted routed Sync styles into `src/styles/sync.css` so the page shell, topbar, and embedded BitCraft Sync iframe treatment have a page-owned stylesheet.
- Extracted routed Map styles into `src/styles/map.css` so the page shell, player/resource selectors, focus banner, map iframe, and Map-specific responsive layout rules have a page-owned stylesheet while admin-only map URL diagnostics stay global.
- Extracted routed Market styles into `src/styles/market.css` so the page shell, live and analytics grids, command/filter panels, best-seller leaderboard, price finder, buy-order finder, pagination, and Market-specific responsive rules have a page-owned stylesheet while shared command headers and suggestion menu primitives stay global.
- Extracted routed Craft Calculator styles into `src/styles/craftcalc.css` so the page shell, lookup controls, route cards, summary cards, material rows, step cards, warning state, and empty state have a page-owned stylesheet while shared command header, suggestion menu, and filter-field primitives stay global.
- Extracted routed Members styles into `src/styles/members.css` so the page shell, roster summary, member table treatment, public-profile equipment panels, gear presets, passive-craft list, and responsive Members rules have a page-owned stylesheet while shared item primitives stay global.
- Extracted routed Professions/Skills styles into `src/styles/skills.css` so the page shell, focus and coverage panels, adventure skill cards, heatmap table treatment, tier legend, and responsive Skills rules have a page-owned stylesheet while shared summary, stat, tier badge, and legend primitives stay global.
- Extracted routed Dashboard styles into `src/styles/dashboard.css` so the page shell, KPI metrics, dashboard cards, feed/member/production/alert lists, legacy dashboard top-meta chrome, and Dashboard responsive rules have a focused stylesheet.
- Extracted dedicated `/bot` dashboard shell, overview, navigation, and responsive shell styles into `src/styles/bot-dashboard.css`, with a CSS ownership test preventing those standalone selectors from drifting back into `styles.css`.
- Added `src/notifications/userToastSettings.ts` for pure browser toast defaults and normalization so corrupted persisted notification booleans, tone IDs, or volume values fall back to safe browser settings before UI use, account save/load, and playback.
- Added `src/notifications/verificationMatrix.ts` as the tested release notification matrix for all routed main-app pages, all supported browser notification types, page-independent sample notification drafts, the live-source verification checklist, and the dedicated `/bot` exception.
- Added `src/notifications/browserSmoke.ts` as loopback-only browser verification helpers, `src/notifications/useBrowserNotificationSmoke.ts` as the mounted smoke wiring hook, `src/notifications/browserNotificationSourceQueue.ts` as the pure live-source queue adapter, `src/notifications/useBrowserNotificationSources.ts` as the mounted live-source notification hook, and `src/notifications/useToastNotifications.ts` as the mounted toast stack/log/timer hook for the built local smoke app and normal browser notification paths.
- Extracted visitor-security retention, GeoIP provider/cache, account, and license-key setting normalization into `src/server/visitorSecuritySettings.mjs` with focused tests, reducing public/secret visitor-security settings policy inside `server.mjs`.
- Extracted market deal-alert public row formatting and Discord DM payload formatting into `src/server/dealAlerts.mjs` with focused tests, reducing more notification-related formatting code inside `server.mjs`.
- Extracted market deal-watch dashboard setting normalization into `src/server/marketDealWatchSettings.mjs` with focused tests, reducing duplicated deal-watch settings policy inside `server.mjs`.
- Extracted dependency-free scheduled-job schedule parsing, serialization, next-run calculation, labels, registry seeding/repair, stale-run recovery, and status shaping behavior into `src/server/scheduledJobs.mjs` with focused tests, reducing more scheduler policy code inside `server.mjs`.
- Extracted dependency-free market listing normalization, BitJita timestamp coercion, trade/listing matching, and market event source-key helpers into `src/server/marketActivity.mjs` with focused tests, reducing market notification/history policy code inside `server.mjs`.
- Extracted dependency-free production craft identity, output lookup, metrics, and profession-key helpers into `src/server/productionActivity.mjs` with focused tests, reducing production notification/activity policy code inside `server.mjs`.
- Extracted dependency-free recipe catalog key, item-kind, detail-target, and cached-row normalization helpers into `src/server/recipeCatalog.mjs` with focused tests, reducing recipe-cache policy code inside `server.mjs`.
- Extracted Discord settings defaults and role-panel, welcome-flow, presence, notification-channel, and colour-role normalization into `src/server/discordSettings.mjs` with focused tests, reducing Discord configuration policy code inside `server.mjs`.
- Extracted background collector defaults, payload-domain ownership maps, current-table ownership maps, and collector setting normalization into `src/server/collectorSettings.mjs` with focused tests, reducing collector configuration policy code inside `server.mjs`.
- Updated `docs/developer-guide.md` and `docs/application-overview.md` so the current AppShell, notification, server-helper, shared browser-helper, and focused stylesheet boundaries are visible outside the audit log.
- Extracted server route classification, visitor logging classification, security headers, frontend MIME type mapping, static cache policy, JSON/text/binary response writers, cookie parsing, HttpOnly session-cookie serialization, request-origin derivation, same-origin checks, safe return-path handling, admin CSRF token matching, body-size limits, raw request-body reading, JSON parsing, body-too-large errors, route rate-limit policies, request-address extraction, 429 limiter behavior, visitor IP normalization, coarse anonymization, and app-salted visitor IP hashing into focused `src/server/` helper modules with focused tests, giving the first small dependency-free server modules before larger route/auth/database splits.
- Extracted BitJita proxy cache policy, admin permission policy, public user response shaping, admin mutation guards, auth identity helpers, session policy helpers, session lookup helpers, Discord OAuth config helpers, OAuth flow helpers, OAuth state helpers, schema bootstrap SQL helpers, prepared-statement factory helpers, default owner admin bootstrap helpers, schema migration metadata/application/cleanup helpers, default app setting bootstrap/application helpers, app settings policy helpers, password-auth helpers, and admin login-attempt helpers into focused `src/server/` modules with focused tests, reducing more release-sensitive policy inside `server.mjs`.

## Architecture Decisions

- Keep `main.tsx` as the startup boundary only.
- Keep notification source hooks mounted from `AppShell` so in-app toasts are not tied to visible page components, and keep shared toast/log/timer behavior in `src/notifications/useToastNotifications.ts`.
- Keep browser notification smoke helpers and hooks loopback-only and sample-data based; they are verification aids for the local built app, not production user-facing controls.
- Keep page-owned pure helpers beside page folders before extracting full components.
- Keep `server.mjs` monolithic until each moved route group has injection-friendly helpers and route/persistence tests that preserve auth, CSRF, cache, and background-job behavior.
- Keep normal page rendering on live BitJita data through the local proxy; SQLite remains the source for history, notifications, analytics, diagnostics, cached tools, and admin/server state.
- Prefer last-known-good or cached helper data only when the UI clearly shows freshness or stale state.

## Server Architecture Audit Notes

`server.mjs` is still the largest release-readiness risk. The current file has several stable conceptual regions that can become modules, but most of them share the same SQLite handle, prepared statements object, settings helpers, rate-limit maps, cache maps, and request helpers. Splitting by copying chunks would be risky; the safer path is dependency-injected modules that receive a small server context instead of importing global state directly. The first extracted helpers, `src/server/httpRoutes.mjs`, `src/server/httpResponses.mjs`, `src/server/httpCookies.mjs`, `src/server/httpRequests.mjs`, `src/server/httpCsrf.mjs`, `src/server/httpBodies.mjs`, `src/server/httpRateLimit.mjs`, `src/server/visitorIp.mjs`, `src/server/visitorSecuritySettings.mjs`, `src/server/notificationActivity.mjs`, `src/server/dealAlerts.mjs`, `src/server/marketDealWatchSettings.mjs`, `src/server/marketActivity.mjs`, `src/server/productionActivity.mjs`, `src/server/recipeCatalog.mjs`, `src/server/discordSettings.mjs`, `src/server/collectorSettings.mjs`, `src/server/scheduledJobs.mjs`, `src/server/bitjitaProxyCache.mjs`, `src/server/adminPermissions.mjs`, `src/server/publicUsers.mjs`, `src/server/adminRequestGuards.mjs`, `src/server/authIdentity.mjs`, `src/server/serverSessions.mjs`, `src/server/sessionLookups.mjs`, `src/server/discordOAuthConfig.mjs`, `src/server/discordOAuthFlow.mjs`, `src/server/oauthState.mjs`, `src/server/schemaBootstrap.mjs`, `src/server/preparedStatements.mjs`, `src/server/defaultOwnerAdmin.mjs`, `src/server/schemaMigrations.mjs`, `src/server/defaultAppSettings.mjs`, `src/server/appRelease.mjs`, `src/server/appSettingsPolicy.mjs`, `src/server/passwordAuth.mjs`, and `src/server/adminLoginAttempts.mjs`, are intentionally dependency-light and own route classification, static-asset visitor logging decisions, security headers, static cache policy, frontend MIME type mapping, JSON/text/binary response writers, cookie parsing, HttpOnly session-cookie serialization, request-origin derivation, same-origin checks, safe return-path handling, admin CSRF token matching, body-size limits, raw request-body reading, JSON parsing, body-too-large errors, route rate-limit policies, request-address extraction, 429 limiter behavior, visitor IP normalization, coarse anonymization, app-salted visitor IP hashing, visitor-security settings normalization, public notification metadata redaction, public deal-alert/Discord payload formatting, market deal-watch settings normalization, market activity normalization/source-key logic, production craft identity/metrics/profession-key logic, recipe catalog target normalization, Discord settings normalization, collector settings normalization, scheduled-job schedule parsing/serialization/labels plus registry seeding/repair, stale-run recovery, and status shaping behavior, BitJita proxy cache TTL policy, pruning, timeout fetches, in-flight dedupe, stale-if-error fallback, and admin role labels, role normalization, least-privilege route permission mapping, permission checks, Discord avatar URL construction, public admin/app user response shaping, admin same-origin/CSRF mutation rejection, admin username validation, Discord ID validation, Discord profile display-name fallback, admin/app session cookie/token policy, DB-backed session lookup policy, Discord OAuth config resolution policy, Discord OAuth authorize/callback/token-profile-request/profile-account/success-redirect policy, Discord OAuth state secret/cookie policy, schema bootstrap SQL policy, prepared-statement factory policy, default owner admin bootstrap policy, schema migration metadata/application/cleanup policy, default app setting bootstrap/application policy, admin settings validation policy, legacy password minimum-length/hash/verify policy, and admin password-login attempt throttling policy.

Current evidence:

- Environment, data-directory setup, SQLite schema creation, migrations, seeded defaults, and prepared statements live near the top of `server.mjs`. The prepared statement block mixes settings, admin sessions, app user sessions, analytics, production, market, Discord delivery, moderation, scheduled jobs, and cached payloads.
- Route classification, static-asset visitor logging, security headers, frontend MIME types, and static cache policy now live in `src/server/httpRoutes.mjs`; JSON/text/binary response writers now live in `src/server/httpResponses.mjs`; cookie parsing and HttpOnly session-cookie serialization now live in `src/server/httpCookies.mjs`; request-origin, same-origin, and safe-return-path helpers now live in `src/server/httpRequests.mjs`; CSRF token derivation and header matching now live in `src/server/httpCsrf.mjs`; body limits, raw request reading, JSON parsing, and body-too-large errors now live in `src/server/httpBodies.mjs`; rate-limit policies, request-address extraction, and 429 limiter behavior now live in `src/server/httpRateLimit.mjs`; visitor IP normalization, coarse anonymization, and app-salted IP hash creation now live in `src/server/visitorIp.mjs`; visitor-security retention, GeoIP provider/cache, account, and license-key setting normalization now lives in `src/server/visitorSecuritySettings.mjs`; public notification-activity metadata redaction now lives in `src/server/notificationActivity.mjs`; market deal-alert row formatting and Discord payload formatting now live in `src/server/dealAlerts.mjs`; market deal-watch setting normalization now lives in `src/server/marketDealWatchSettings.mjs`; market listing normalization, BitJita timestamp coercion, trade/listing matching, and market event source-key helpers now live in `src/server/marketActivity.mjs`; production craft identity, output lookup, metrics, and profession-key helpers now live in `src/server/productionActivity.mjs`; recipe catalog key and target normalization helpers now live in `src/server/recipeCatalog.mjs`; Discord settings defaults and normalization helpers now live in `src/server/discordSettings.mjs`; collector defaults, payload-domain ownership maps, current-table ownership maps, and collector settings normalization now live in `src/server/collectorSettings.mjs`; scheduled-job schedule parsing, serialization, next-run calculation, labels, registry seeding/repair, stale-run recovery, and status shaping behavior now live in `src/server/scheduledJobs.mjs`; BitJita proxy cache policy lives in `src/server/bitjitaProxyCache.mjs`; admin permission policy lives in `src/server/adminPermissions.mjs`; public user response shaping lives in `src/server/publicUsers.mjs`; admin mutation guards live in `src/server/adminRequestGuards.mjs`; auth identity validation and display-name fallback live in `src/server/authIdentity.mjs`; admin/app session cookie names, lifetimes, token hashing, token extraction, session creation payloads, and clear-cookie helpers live in `src/server/serverSessions.mjs`; DB-backed admin/app session lookup lives in `src/server/sessionLookups.mjs`; Discord OAuth client/secret/redirect config resolution lives in `src/server/discordOAuthConfig.mjs`; Discord OAuth authorize URL construction, callback decision, token/profile request descriptors, profile account mapping, and success redirect/cookie helpers live in `src/server/discordOAuthFlow.mjs`; Discord OAuth state secret reuse/generation, signing, tamper checks, return-path clamping, and state-cookie helpers live in `src/server/oauthState.mjs`; SQLite schema bootstrap SQL lives in `src/server/schemaBootstrap.mjs`; SQLite prepared statement creation lives in `src/server/preparedStatements.mjs`; default owner Discord admin seeding lives in `src/server/defaultOwnerAdmin.mjs`; additive schema migration columns, legacy schema cleanup, release-sensitive bootstrap indexes, and migration application helpers live in `src/server/schemaMigrations.mjs`; default app setting seed rows, insertion, and obsolete setting cleanup live in `src/server/defaultAppSettings.mjs`; admin settings validation and saved-setting normalization for claim IDs, refresh intervals, retention days, region IDs, stored/submitted excluded members, default pages, and BitCraft Sync URLs lives in `src/server/appSettingsPolicy.mjs`; legacy password minimum-length policy, hashing, and verification live in `src/server/passwordAuth.mjs`; admin password-login attempt keying, failure windows, and success clearing live in `src/server/adminLoginAttempts.mjs`. Focused tests cover admin, auth, Discord, BitJita proxy, local API, static asset, app-route classifications, release security headers, frontend asset response policy, response writer headers/bodies, cookie parsing, cookie attributes, origin derivation, same-origin behavior, redirect path safety, CSRF token matching, body-size limit policies, streamed body concatenation, empty-body JSON defaults, 413 body-too-large errors, request-address selection, public route rate-limit policies, bucket reset behavior, 429 retry headers, loopback/IPV4-mapped normalization, IPv4/IPv6 anonymization, app-salted visitor IP hashing, visitor-security setting normalization, recursive sensitive-key notification metadata redaction, deal-alert public/Discord payload formatting, market deal-watch settings normalization, market listing identity/timestamp normalization, trade/listing matching, market event source-key behavior, production craft stable identity, blank-ID fallback, production metrics/profession-key behavior, recipe catalog key/target normalization, Discord settings defaults/normalization, collector settings normalization/domain maps, scheduled-job schedule parsing, clamping, serialization, next-run, label behavior, registry seeding/repair, stale-run recovery, and status shaping behavior, BitJita proxy cache policy/dedupe/stale/pruning behavior, admin role/permission mapping behavior, public admin/app user payload behavior, admin mutation guard behavior, auth identity validation/display-name fallback behavior, session cookie/token policy behavior, DB-backed session lookup behavior, Discord OAuth config resolution behavior, Discord OAuth authorize/callback/token-profile-request/profile-account/success-redirect behavior, OAuth state secret reuse/generation and signing/cookie policy behavior, schema bootstrap SQL behavior, prepared-statement factory behavior, default owner admin bootstrap behavior, schema migration metadata/application/cleanup behavior, default app setting seed-row behavior, default insert/delete application, obsolete setting cleanup keys, admin settings validation and saved-setting normalization behavior, legacy password minimum-length/hash/verify behavior, and admin login-attempt throttling behavior.
- Authentication and security helpers are partly extracted: admin role labels, role normalization, route permission mapping, and permission checks live in `src/server/adminPermissions.mjs`; public admin/app user payload shaping lives in `src/server/publicUsers.mjs`; admin mutation rejection lives in `src/server/adminRequestGuards.mjs`; admin username validation, Discord ID validation, and Discord profile display-name fallback live in `src/server/authIdentity.mjs`; admin/app session cookie/token policy lives in `src/server/serverSessions.mjs`; Discord OAuth authorize/callback/token-profile-request/profile-account/success-redirect policy lives in `src/server/discordOAuthFlow.mjs`; Discord OAuth state secret/cookie policy lives in `src/server/oauthState.mjs`; additive schema migration metadata, cleanup, and application helpers live in `src/server/schemaMigrations.mjs`; default app setting bootstrap/application policy lives in `src/server/defaultAppSettings.mjs`; admin settings validation policy lives in `src/server/appSettingsPolicy.mjs`; legacy password minimum-length/hash/verify policy lives in `src/server/passwordAuth.mjs`; admin login-attempt throttling policy lives in `src/server/adminLoginAttempts.mjs`. Password login/setup route flow and Discord OAuth network/profile/session side effects remain clustered in `server.mjs` for a later dependency-injected split.
- Discord behavior is large and mixed: message delivery, role panels, moderation, custom commands, component handling, OAuth-backed admin/user state, and interaction handling are in one file but mostly grouped by function families.
- Collector and scheduled-job behavior is also grouped enough to split later: recipe catalog refresh, GeoIP refresh, market deal watch jobs, regional buy-order baselines, production contribution collection, storage activity, current-claim refresh, and server snapshot polling are identifiable domains.
- The main `createServer` dispatcher is an ordered route chain. Public health/proxy/config endpoints must remain before admin routes; static frontend fallback must remain after API handling so API typos do not return `index.html`.

Current automated coverage that protects a future split:

- `test/server-route-groups.test.mjs` covers the extracted route classification helper directly.
- `test/server-http-bodies.test.mjs` covers body-size limits, streamed raw-body reading, JSON parsing, and 413 body-too-large errors directly.
- `test/server-http-rate-limit.test.mjs` covers request-address extraction, route rate-limit policies, bucket windows, and 429 retry response behavior directly.
- `test/server-bitjita-proxy-cache.test.mjs` covers BitJita proxy TTL policy selection, upstream request headers, timeout signal wiring, fresh-cache hits, in-flight dedupe, stale-if-error fallback, and cache pruning directly.
- `test/server-admin-permissions.test.mjs` covers admin role labels, safe role fallback, scoped role permissions, owner wildcard behavior, and least-privilege admin route permission mapping directly.
- `test/server-public-users.test.mjs` covers Discord avatar URL construction, public admin-user payloads, public app-user payloads, and malformed app-user settings fallback directly.
- `test/server-admin-request-guards.test.mjs` covers read-only admin requests, cross-origin mutation rejection, exact CSRF token matching, and local dev loopback same-origin allowance directly.
- `test/server-auth-identity.test.mjs` covers admin username validation, Discord ID validation, and Discord profile display-name fallback directly.
- `test/server-sessions.test.mjs` covers admin/app session cookie names, lifetimes, token hashing, session payload creation, request token extraction, and clear-cookie behavior directly.
- `test/server-session-lookups.test.mjs` covers missing-cookie behavior, expired-session cleanup, hashed-token lookup, timestamp reuse, and null lookup-row normalization directly.
- `test/server-discord-oauth-config.test.mjs` covers Discord OAuth env/settings client ID precedence, env/stored secret precedence, redirect URI fallback, trimming, and enabled/disabled state directly.
- `test/server-discord-oauth-flow.test.mjs` covers Discord OAuth authorize URL parameters, denied/error callback redirects, safe return-path fallback, matching-state acceptance, token/profile fetch request shape, token exchange form fields, profile account mapping, and success redirect cookie ordering directly.
- `test/server-oauth-state.test.mjs` covers Discord OAuth state secret reuse/generation, state cookie policy, state signing, tamper rejection, return-path clamping, signed-cookie reading, and clear-cookie behavior directly.
- `test/server-app-settings-policy.test.mjs` covers claim ID, saved/submitted refresh interval, saved/submitted retention day, region ID, stored/submitted excluded-member normalization, app default-page allow-list, and BitCraft Sync URL validation directly.
- `test/server-password-auth.test.mjs` covers legacy password minimum length, scrypt password hash format, matching-password verification, wrong-password rejection, and malformed stored password rejection directly.
- `test/server-admin-login-attempts.test.mjs` covers admin login-attempt keying, five-failure limit, fifteen-minute expiry, expired failure reset, and success clearing directly.
- `test/server-visitor-ip.test.mjs` covers visitor IP normalization, anonymization, and app-salted hash format directly.
- `test/server.test.mjs` starts the real server in a child process with an isolated data directory and mocked upstream BitJita service.
- The integration test covers security headers, BitJita proxy cache/dedupe/stale-if-error behavior, cached local helper endpoints with freshness metadata, admin setup/login/roles/CSRF, scheduled jobs, GeoIP, analytics, market deal watches and alerts, polling, activity/history, production data protection, notification-activity filtering, notification metadata redaction, and the extracted notification-activity helper boundary.
- A separate background-polling test verifies upstream failures do not terminate the server process.

Recommended split order:

1. Move SQLite schema/migrations and prepared statements into a database module that returns `{ db, statements }`; do not let route modules open their own database handles.
2. Admin role/permission policy is now extracted into `src/server/adminPermissions.mjs`, public user payload shaping is extracted into `src/server/publicUsers.mjs`, admin mutation guards are extracted into `src/server/adminRequestGuards.mjs`, auth identity helpers are extracted into `src/server/authIdentity.mjs`, session policy helpers are extracted into `src/server/serverSessions.mjs`, session lookup helpers are extracted into `src/server/sessionLookups.mjs`, Discord OAuth config helpers are extracted into `src/server/discordOAuthConfig.mjs`, OAuth flow helpers are extracted into `src/server/discordOAuthFlow.mjs`, OAuth state secret/cookie helpers are extracted into `src/server/oauthState.mjs`, app settings policy helpers are extracted into `src/server/appSettingsPolicy.mjs`, password-auth helpers are extracted into `src/server/passwordAuth.mjs`, and admin login-attempt helpers are extracted into `src/server/adminLoginAttempts.mjs`; continue later with OAuth network/database/session side-effect helpers using dependency-injected tests for owner/admin/viewer behavior and rejection paths.
3. BitJita proxy/cache policy is now extracted into `src/server/bitjitaProxyCache.mjs`; keep the route handler thin and covered by focused helper tests plus the existing proxy integration test.
4. Extract scheduled jobs and collectors behind a small service API so long-running work remains outside request lifetimes.
5. Extract Discord service functions separately from admin route handlers; keep real Discord delivery mocked or disabled in tests.
6. Split the route dispatcher last into small route modules registered in the same order as today.

Do not split `server.mjs` further until a fresh server test run passes after each step. Any route extraction should keep admin mutations behind `requireAdmin`, `requireAdminMutation`, and `requireAdminPermission`; preserve the public/private distinction for app user endpoints; keep bot tokens and OAuth secrets out of public settings responses; and keep expensive API-heavy work behind cache/rate-limit/background-job boundaries.

## CSS And UX Audit Notes

Evidence from the current stylesheet shows reusable tokens and focus rules already exist, including CSS variables for colors, radii, control heights, focus rings, and z-indexes. The active CSS entrypoints are imported from `src/main.tsx` in this order: `src/styles/setup-workflow.css`, `src/styles.css`, `src/styles/admin.css`, `src/styles/discord-admin.css`, `src/styles/dashboard.css`, `src/styles/leaderboard.css`, `src/styles/production.css`, `src/styles/public-craft.css`, `src/styles/market.css`, `src/styles/craftcalc.css`, `src/styles/members.css`, `src/styles/skills.css`, `src/styles/inventory.css`, `src/styles/construction.css`, `src/styles/research.css`, `src/styles/activity.css`, `src/styles/region.css`, `src/styles/sync.css`, `src/styles/map.css`, `src/styles/bot-dashboard.css`, `src/styles/empires.css`, `src/styles/app-chrome.css`, `src/styles/user-settings.css`, and `src/styles/notifications.css`. Current module sizes are `styles.css` at 1470 lines, `admin.css` at 792 lines, `discord-admin.css` at 388 lines, `setup-workflow.css` at 164 lines, `dashboard.css` at 353 lines, `leaderboard.css` at 164 lines, `production.css` at 384 lines, `public-craft.css` at 122 lines, `market.css` at 798 lines, `craftcalc.css` at 231 lines, `members.css` at 216 lines, `skills.css` at 320 lines, `inventory.css` at 243 lines, `construction.css` at 390 lines, `research.css` at 271 lines, `activity.css` at 334 lines, `region.css` at 211 lines, `sync.css` at 25 lines, `map.css` at 166 lines, `bot-dashboard.css` at 45 lines, `empires.css` at 136 lines, `app-chrome.css` at 103 lines, `user-settings.css` at 98 lines, and `notifications.css` at 81 lines.

Sidebar, tables, broad responsive rules, browser theme propagation, and shared admin and Discord/bot admin styles now live outside `styles.css`; routed Dashboard styles now live in `src/styles/dashboard.css`; routed Leaderboard styles now live in `src/styles/leaderboard.css`; routed Production styles now live in `src/styles/production.css`; routed Public Craft Finder styles now live in `src/styles/public-craft.css`; routed Market styles now live in `src/styles/market.css`; routed Craft Calculator styles now live in `src/styles/craftcalc.css`; routed Members styles now live in `src/styles/members.css`; routed Professions/Skills styles now live in `src/styles/skills.css`; routed Inventory styles now live in `src/styles/inventory.css`; routed Construction styles now live in `src/styles/construction.css`; routed Research styles now live in `src/styles/research.css`; routed Activity styles now live in `src/styles/activity.css`; routed Region styles now live in `src/styles/region.css`; routed Sync styles now live in `src/styles/sync.css`; routed Map styles now live in `src/styles/map.css`; routed Empires scouting and watchtower styles now live in `src/styles/empires.css`; floating tools, shared help/legal dialogs, Discord sign-in dialog, cookie consent, and command palette shell styles now live in `src/styles/app-chrome.css`; notification toast/drawer/sound styles live in `src/styles/notifications.css`; browser settings/theme-editor styles live in `src/styles/user-settings.css`; shared admin console styles live in `src/styles/admin.css`; Discord/bot admin section styles live in `src/styles/discord-admin.css`; and the dedicated `/bot` dashboard shell/navigation styles live in `src/styles/bot-dashboard.css`. The app-chrome module preserves the z-index relationship fixed during notification smoke testing: `--z-toast` sits above `--z-help`, while modal overlays still sit above both. A selector ownership scan found the setup/workflow stylesheet was still carrying global mobile sidebar/nav scrollbar selectors; a later pass also replaced legacy `production-command-*` shared primitives and the reused `market-command-header` helper with neutral `command-filter-*` class names; those rules now live with the base mobile nav rules in `styles.css`, and `test/css-ownership.test.mjs` prevents `setup-workflow.css` from regaining broad `aside`, `nav`, `.sidebar-collapsed nav`, or `.bot-section-nav` ownership. The same test now also guards standalone Dashboard selectors so `.dashboard-page`, `.dashboard-topbar`, `.dashboard-kpis`, `.dashboard-metric`, `.dashboard-main-grid`, `.dashboard-feed`, `.dashboard-feed-row`, `.dashboard-member-list`, `.dashboard-production-list`, `.dashboard-alert-list`, and `.dashboard-empty` stay in `dashboard.css`; standalone Leaderboard selectors so `.leaderboard-page`, `.leaderboard-tabs`, `.leaderboard-summary`, `.leaderboard-card`, `.leaderboard-grid`, `.leaderboard-profession-list`, and `.leaderboard-recent-list` stay in `leaderboard.css`; standalone Production selectors so `.production-page`, `.production-topbar`, `.production-summary`, `.production-grid`, `.production-card`, `.production-member-banner`, `.production-page .production-member-banner`, `.settlement-passive-crafts`, `.private-craft-pill`, `.production-private-toggle`, and `.production-crafter-line` stay in `production.css`; standalone Public Craft Finder selectors so `.public-craft-page`, `.public-craft-finder`, `.public-craft-topbar`, `.public-craft-summary`, `.public-craft-command-panel`, `.public-craft-hint`, and `.map-location-link` stay in `public-craft.css`; standalone Market selectors so `.market-page`, `.market-topbar`, `.market-summary`, `.market-command-panel`, `.market-filter-panel`, `.market-tool-row`, `.market-member-field`, `.market-tabs`, `.market-filter-grid`, `.market-analytics`, `.market-best-leaderboard`, `.price-finder`, `.buy-order-opportunities`, `.opportunity-strip`, `.pagination-row`, `.price-recommendation`, `.deal-watch-action`, `.deal-watchlist-section`, `.deal-watch-list`, `.deal-watch-row`, `.deal-watch-meta`, `.deal-watch-actions`, `.deal-watch-threshold`, and `.deal-watch-empty` stay in `market.css`; standalone Craft Calculator selectors so `.craftcalc-page`, `.craftcalc-topbar`, `.craftcalc-controls`, `.craftcalc-recipe-picker`, `.craftcalc-control-grid`, `.craftcalc-route-list`, `.craftcalc-route-card`, `.craftcalc-route-heading`, `.craftcalc-route-pill`, `.craftcalc-summary`, `.craftcalc-section`, `.craftcalc-material-grid`, `.craftcalc-material-row`, `.craftcalc-step-list`, `.craftcalc-step-card`, `.craftcalc-warning`, and `.craftcalc-empty` stay in `craftcalc.css`; standalone Members selectors so `.members-page`, `.members-topbar`, `.members-summary-grid`, `.members-toolbar`, `.members-roster-table`, `.member-name-cell`, `.member-row-avatar`, `.member-row-copy`, `.profile-actions`, `.gear-preset-list`, `.gear-preset`, `.public-profile-grid`, `.profile-history-panel`, `.profile-section-heading`, `.passive-craft-list`, and `.passive-craft-card` stay in `members.css`; standalone Skills selectors so `.skills-page`, `.skills-topbar`, `.skills-summary`, `.skills-dashboard`, `.focus-panel`, `.coverage-panel`, `.focus-metrics`, `.focus-tier-strip`, `.focus-tier-segment`, `.focus-list`, `.coverage-list`, `.adventure-skills-panel`, `.adventure-skill-grid`, `.skills-toolbar`, `.heatmap-wrap`, `.skill-table`, `.skill-cell`, and `.tier-legend` stay in `skills.css`; standalone Inventory selectors so `.inventory-page`, `.inventory-topbar`, `.inventory-summary`, `.inventory-command-header`, `.inventory-command-actions`, `.inventory-filter-grid`, `.inventory-filter-field`, and `.inventory-inline-toggle` stay in `inventory.css`; standalone Construction selectors so `.construction-page`, `.construction-topbar`, `.construction-summary`, `.construction-section-heading`, `.construction-need-card`, `.construction-controls`, `.construction-sort-field`, `.construction-material-list`, `.construction-material-row`, and `.construction-complete-toggle` stay in `construction.css`; standalone Research selectors so `.research-panel`, `.research-topbar`, `.research-summary`, `.research-unlocks`, `.research-command-panel`, `.research-command-header`, `.research-filter-grid`, `.research-lanes`, and `.research-card` stay in `research.css`; standalone Activity selectors so `.activity-panel`, `.activity-topbar`, `.activity-overview`, `.activity-command-panel`, `.activity-command-head`, `.activity-filter-grid`, `.activity-filters`, `.activity-options`, `.activity-timeline`, `.activity-event`, `.activity-search-loading`, and `.activity-empty` stay in `activity.css`; standalone Region selectors so `.region-panel`, `.region-topbar`, `.region-rank-grid`, `.region-summary-grid`, `.region-insights`, `.region-context`, `.region-leaders-panel`, `.region-table-panel`, `.nearby-panel`, `.mine-row`, and `.mine-text` stay in `region.css`; standalone Sync selectors so `.sync-panel`, `.sync-topbar`, and `.sync-frame` stay in `sync.css`; standalone Map selectors so `.map-panel.full-height`, `.map-topbar`, `.map-frame`, `.map-focus`, `.map-workspace`, `.map-resource-panel`, `.map-resource-heading`, `.map-resource-controls`, `.map-selected-resources`, `.map-resource-list`, and `.map-resource-icon` stay in `map.css`; and standalone `/bot` dashboard shell selectors so `.bot-control-page`, `.bot-console`, `.bot-dashboard`, `.bot-overview`, `.bot-layout`, `.bot-section-nav`, `.bot-nav-title`, and `.bot-nav-group` stay in `bot-dashboard.css` rather than `styles.css`; shared admin selectors stay in `admin.css`; and standalone Discord/bot admin section selectors stay in `discord-admin.css`. The same test also prevents the old `production-command-panel`, `production-command-main`, `production-command-title`, and `market-command-header` shared class names from returning to checked source files. A 2026-06-28 built-app CSS/UX smoke pass checked Dashboard, Market, Inventory, Empires, Admin, and `/bot` at the current desktop viewport plus Dashboard, Market, Empires, and `/bot` at 390x844. Those routes rendered nonblank content, reported no browser console errors, had no root-level horizontal overflow, and kept dense table overflow inside `.table-wrap` containers where applicable. A 2026-06-29 built-app smoke pass also checked Leaderboard and Production at 1440x1000 and 390x844 after the page-owned stylesheet extraction; both routed pages rendered their page shells and content, reported no browser console errors, and had no root-level horizontal overflow. A later Leaderboard smoke after extracting `src/pages/LeaderboardPage.tsx` loaded `/?page=leaderboard`, rendered the Leaderboard shell, active nav, five tabs, four summary cards, four leaderboard cards, reported no browser console errors, and had no root-level horizontal overflow. A later Public Craft Finder smoke after extracting `src/styles/public-craft.css` loaded `/?page=publiccrafts`, rendered the page shell, finder container, and summary section, reported no browser console errors, and had no root-level horizontal overflow; the table was not present in that local smoke data state, so populated-table visual verification remains covered by build/CSS ownership checks rather than that browser pass. A follow-up Inventory smoke after extracting `src/styles/inventory.css` loaded `/?page=inventory`, rendered the page shell, summary, filter grid, non-empty toggle, material watch, and container list, reported no browser console errors, and had no root-level horizontal overflow. A Construction smoke after extracting `src/styles/construction.css` loaded `/?page=construction`, rendered the page shell, summary, controls, and project-list/empty-state area, reported no browser console errors, and had no root-level horizontal overflow. A Research smoke after extracting `src/styles/research.css` loaded `/?page=research`, rendered the page shell, active nav, five summary cards, unlock chips, filter panel, research lanes, and 146 technology cards, reported no browser console errors, and had no root-level horizontal overflow. An Activity smoke after extracting `src/styles/activity.css` loaded `/?page=activity`, rendered the page shell, active nav, four summary cards, filter panel, seven category buttons, timeline, and 54 activity events, reported no browser console errors, and had no root-level horizontal overflow. A later Activity smoke after extracting `src/pages/ActivityPage.tsx` loaded `/?page=activity`, rendered the Activity shell, active nav, four summary cards, seven category buttons, timeline, and 54 activity events, reported no browser console errors, and had no root-level horizontal overflow. A Region smoke after extracting `src/styles/region.css` loaded `/?page=empire`, rendered the Region page shell, active nav, four rank cards, five summary cards, regional insights, 15 supply leader rows, five nearby settlement rows, and 100 ranking table rows, reported no browser console errors, and had no root-level horizontal overflow. A Sync smoke after extracting `src/styles/sync.css` loaded `/?page=sync`, rendered the Sync page shell, active nav, topbar, and embedded BitCraft Sync iframe at 713x720, reported no browser console errors, and had no root-level horizontal overflow. A Map smoke after extracting `src/styles/map.css` loaded `/?page=map`, rendered the Map page shell, active nav, 12 player filter pills, resource finder panel/list, and embedded BitCraft World Map iframe at 737x529, reported no browser console errors, and had no root-level horizontal overflow. A Market smoke after extracting `src/styles/market.css` loaded `/?page=market` and `/?page=market&tab=live`, rendered the Market shell, active nav, four summary cards, four market tabs, price finder, live filter panel, four live grid cards, three highlight cards, one contained table with 20 rows, reported no browser console errors, and had no root-level horizontal overflow. A 2026-06-29 bounded built-app CSS/UX audit checked Dashboard, Market, Production, Admin, and `/bot` at 1280x720; all sampled routes reported no browser console errors and no root-level horizontal overflow. That pass found the shared table sort buttons were only about 14px tall on the populated Market table, so `.table-sort-button` now has a 28px minimum height and 6px horizontal padding; the follow-up Market browser check confirmed eight visible sort buttons at 28px tall, no horizontal overflow, and no console errors. A later routed-page pass checked Leaderboard, Members, Professions/Skills, Inventory, Construction, Research, Region, Sync, Craft Calculator, and Public Craft Finder at the default desktop viewport and 390x844; no sampled page reported browser console errors or root horizontal overflow, and table-heavy pages kept overflow in their scroll containers. That pass found Public Craft Finder table sort buttons and map location links at 14-17px tall on mobile, so shared `.sort-button` and Public Craft Finder `.map-location-link` targets now have 28px minimum heights; the follow-up mobile browser check confirmed visible Public Craft Finder sort/location actions at 28px or taller with no root overflow or console errors. A follow-up 2026-06-29 admin/bot state audit checked the unauthenticated Admin Console and `/bot` verification screens at 1280x720 and 390x844; both surfaces rendered explicit verifying-access/session-role status text, had no browser console errors, and no root-level horizontal overflow. That pass found shared app-chrome controls at 24-26px tall, so the sidebar collapse button, sidebar section toggles, and footer links now use 30px click targets; the follow-up browser check confirmed no undersized shared chrome controls on Admin desktop/mobile and no root overflow or console errors on Admin or `/bot`. `test/css-ownership.test.mjs` guards the shared table-sort and app-chrome click-target rules. A Craft Calculator smoke after extracting `src/styles/craftcalc.css` loaded `/?page=craftcalc`, rendered the Craft Calculator shell, active nav, lookup controls with two fields, and empty state, reported no browser console errors, and had no root-level horizontal overflow. A Members smoke after extracting `src/styles/members.css` loaded `/?page=members`, rendered the Members shell, active nav, four summary cards, search toolbar, roster table with 10 rows, and contained roster-table overflow, reported no browser console errors, and had no root-level horizontal overflow at the normal browser viewport. The 390x844 Members mobile smoke initially exposed clipped page content caused by the roster table's min-content width; `members.css` now lets page children shrink while the roster table scrolls internally, and the follow-up mobile smoke confirmed the summary/topbar reflowed to the 323px content width, table overflow stayed inside `.table-wrap`, root/body overflow stayed false, and console errors were empty. A Skills smoke after extracting `src/styles/skills.css` loaded `/?page=skills`, rendered the routed Professions shell, active nav, four summary cards, focus and coverage panels, toolbar, and two heatmap tables with no console errors. The 390x844 Skills mobile smoke initially exposed clipped page content caused by the heatmap table min-content width; `skills.css` now lets page children shrink while the heatmaps scroll internally, and the follow-up mobile smoke confirmed the summary/topbar reflowed to the 323px content width, both 1370px heatmaps stayed inside 323px `.heatmap-wrap` containers, root/body overflow stayed false, and console errors were empty. A Dashboard smoke after extracting `src/styles/dashboard.css` and `src/pages/DashboardPage.tsx` loaded `/?page=dashboard`, rendered the Dashboard shell, active nav, five KPI cards, six dashboard cards, feed/member/production/alert lists, and no browser console errors. The 390x844 Dashboard mobile smoke confirmed the KPI and dashboard grids collapsed to one 343px column, the expected supply/treasury/activity/members/production/attention grid order applied, root/body overflow stayed false, and console errors were empty.

Remaining CSS work:

- Identify unused or duplicate selectors before deletion.
- Split stable page-specific sections only after the related page/component boundary is clear.
- Keep future extractions feature-owned and descriptively named; use `setup-workflow.css`, `admin.css`, `discord-admin.css`, `dashboard.css`, `leaderboard.css`, `production.css`, `public-craft.css`, `inventory.css`, `construction.css`, `research.css`, `activity.css`, `region.css`, `sync.css`, `map.css`, `market.css`, `craftcalc.css`, `members.css`, `skills.css`, `bot-dashboard.css`, `notifications.css`, `user-settings.css`, and `empires.css` as the current pattern, and do not create more phase-numbered modules.
- Continue visual screenshot review for the main dashboard pages and bot/admin pages. App-chrome, representative page/table/admin/bot desktop smoke checks, and 390px mobile smoke checks now have evidence; deeper screenshot review for all key pages and states is still required.
- Check contrast, focus states, disabled/loading states, stale-data states, and table overflow on desktop and mobile.

## Notification Audit Notes

Supported browser in-app notification types are documented in `docs/notification-system.md`:

- Market listing toasts.
- Market sale toasts.
- Market deal alert toasts.
- Production started and completed toasts.

Current automated coverage verifies pure generation, settings gating, source-key dedupe, unseen selection, market activity source queueing by stable notification keys, blank activity source-key fallback, signed-in deal-alert source queueing, malformed deal-alert row filtering, per-user signed-in deal-alert dedupe scoping, disabled market-toast gating for deal-alert rows without later replay, non-empty production craft identity fallback, production queue diffing, the app-level source queue adapter that combines notification-activity rows, signed-in deal-alert rows, and production queue diffs without page-mounted state, visible toast-stack caps, persisted notification-log caps, duplicate replacement, drawer read-state marking, browser toast setting normalization, the release verification matrix covering every routed main-app panel, page-independent draft-to-toast/log creation for every supported browser notification type, the live-source checklist with dated evidence metadata for verified sources and tested required-blocker filter that separate smoke evidence from source verification requirements, the loopback-only browser smoke bridge/query parser, and AppShell boundaries that delegate smoke wiring to `src/notifications/useBrowserNotificationSmoke.ts`, live source effects to `src/notifications/useBrowserNotificationSources.ts`, and toast stack/log/timer state to `src/notifications/useToastNotifications.ts`. Browser smoke verification proves market listing and market sale notifications plus drawer persistence on Dashboard, Leaderboard, Members, Professions, Production, Inventory, Construction, Research, Market, Region, Empires, Map, Sync, Activity, Public Craft Finder, Craft Calculator, and Admin. A 2026-06-28 follow-up also verified Dashboard toast creation from a smoke-only market listing, unread badge clearing when the drawer opens, drawer close behavior, drawer notice navigation to Market, drawer availability on Production with market and production entries, and the user-level market-listing setting blocking a later smoke listing while disabled. Later controlled probes verified refresh-triggered market listing toasts appear, stay visible until the timer expires, can be dismissed manually, and remain in the drawer log after dismissal on Dashboard, Market, Activity, Map, Production, and Admin. A later built-app smoke pass used the loopback-only `smokeNotification` query trigger to verify all five supported notification types on all 17 routed main-app pages, for 85/85 visible toast checks with the notification button present, smoke query parameters removed after firing, and no browser console errors. Follow-up probes dismissed production-started and market-deal-alert toasts, then verified those dismissed notices persisted in the Recent notifications drawer alongside production-completed history. Those probes found the floating Help button was stacked above the toast dismiss button; `styles.css` now keeps `--z-toast` above `--z-help` while leaving overlays above both. The dedicated `/bot` route intentionally mounts `BotControlApp` without the main app notification chrome, so bot-route browser notifications are not supported for this release under `accepted-intentional-exception`. Remaining notification blockers are live-source production started/completed generation from actual queue diffs and live-source market deal alerts with a signed-in Discord-linked user. Browser sound Preview now has representative page evidence, but final release should repeat it if notification sound settings change again.

## Security And Config Notes

Current documented safeguards and verified findings:

- Admin pages use server-side sessions and CSRF tokens for mutations.
- Admin routes have role permissions; the `admin` role now includes `users.manage`, and server tests verify an `admin` user can list admin users while a `viewer` user receives `403`.
- Discord admin login is the normal production path; legacy password setup is compatibility-only.
- Discord bot tokens should come from environment variables or protected `app_secrets`, not public settings responses; server tests now verify a submitted bot token is stored as configured but omitted from both the immediate PUT response and the later GET settings response.
- GeoIP license keys are returned to the frontend as configured/not-configured state unless the server explicitly requests secrets internally; server tests verify the configured flag is returned while the raw license key is omitted.
- The admin table browser excludes `app_secrets`.
- Production data lives outside the Git checkout at `/var/lib/bitcraft-claim-monitor`.
- Visitor-security analytics store normalized, coarsely anonymized, and salted-hashed IP values through `src/server/visitorIp.mjs`; focused tests preserve the release privacy behavior for loopback, IPv4-mapped, IPv4, and IPv6 addresses.
- Node should remain bound to localhost behind Caddy in production.
- 2026-06-29 ignored-file scan confirmed local smoke logs, development SQLite files, and test SQLite files remain ignored; dependency-tree icon filename matches were excluded from the final reviewed list.
- 2026-06-29 tracked-file scan found no SQLite databases, log files, private keys, certificate bundles, backups, uploads, branding assets, `.codex-dev/`, or `outputs/` committed.
- 2026-06-29 token/private-key pattern scan found no committed Discord-token, GitHub-token, OpenAI-key, Slack-token, AWS-key, Google-key, or private-key shaped literals. A broader name-based scan only found documented placeholders, server secret plumbing, docs, scripts that read local ignored live-query tokens, and test-only fake secrets.
- 2026-06-29 deployment-doc recheck compared `DEPLOYMENT.md`, `.env.example`, `deploy/bitcraft-claim-monitor.service`, `deploy/Caddyfile.example`, the root package manager policy, `apps/bitcraft-local/package.json`, and the current server environment defaults after the schema/default-setting/default-owner/release-metadata helper extractions. The docs and checked-in deployment files agree on Node 24+, `pnpm@11.1.3`, production binding to `127.0.0.1:18430`, Caddy reverse proxying only to localhost, persistent data at `/var/lib/bitcraft-claim-monitor`, and optional Discord/admin secrets living in `/etc/bitcraft-claim-monitor.env` or protected admin storage.
- 2026-06-28 `/api/local/notification-activity` now redacts sensitive metadata keys before returning public notification rows through `src/server/notificationActivity.mjs`. A focused server integration test inserts a market notification event containing Discord bot token, admin setup key, and OAuth secret sentinels, then verifies the public payload preserves safe item metadata while omitting those secret values.

Remaining security/config work:

- Re-run the targeted secret scan after any further deployment, Discord, auth, settings, or environment-file changes before the final release claim.
- Re-check deployment docs again only after further deployment, environment, port, storage, Discord, or auth changes before the final release claim.

## Verification Status

Most recent verified checks in this release-readiness pass:

- Focused activity helper test: `node --experimental-strip-types --test test/activity-utils.test.mjs` passed with 5 tests after extracting activity-log cleanup into the activity utility module and hardening blank notification source-key fallback.
- Focused production helper test: `node --experimental-strip-types --test test/production-utils.test.mjs` passed after extracting production craft activity-window, progress-key, and metrics helpers.
- Focused notification source tests: `node --experimental-strip-types --test apps/bitcraft-local/test/notifications.test.mjs` passed with 20 tests after extracting market activity, deal-alert, production source queue, app-level source queue, toast-stack, persisted-log, drawer read-state helpers, stable market-activity source-key queueing, per-user signed-in deal-alert dedupe scoping, non-empty production craft identity fallback, disabled deal-alert market-gate behavior, and malformed deal-alert row filtering.
- Focused notification sound/settings tests: `node --experimental-strip-types --test test/notification-sounds.test.mjs` passed with 5 tests after adding persisted setting normalization plus generated-audio coverage for disabled sound and selected enabled tones.
- Focused notification matrix/boundary tests: `node --experimental-strip-types --test test/appshell-notification-boundary.test.mjs test/notification-matrix.test.mjs` passed after keeping AppShell smoke wiring delegated to `src/notifications/useBrowserNotificationSmoke.ts`, live source effects delegated to `src/notifications/useBrowserNotificationSources.ts`, toast stack/log/timer state delegated to `src/notifications/useToastNotifications.ts`, and preserving the tested release checklist for all routed pages, supported browser notification types, page-independent sample draft-to-toast/log creation for every supported browser notification type, unique page-scoped source keys for all 85 routed page/type combinations, one live-source checklist row per supported type, dated evidence metadata for verified live sources, the required/verified live-source status filter, the loopback-only smoke bridge/query parser, and the `/bot` accepted intentional exception.
- Focused AppShell user-settings boundary test: `node --experimental-strip-types --test test/appshell-user-settings-boundary.test.mjs` passed after moving browser-local settings UI into `src/components/main/UserSettingsDialog.tsx` and importing the dialog from `src/AppShell.tsx`.
- Focused AppShell admin boundary test: `node --experimental-strip-types --test test/appshell-admin-boundary.test.mjs` passed after moving admin console rendering into `src/components/admin/AdminPanel.tsx` and importing it from `src/AppShell.tsx`.
- Focused AppShell import-boundary test: `node --experimental-strip-types --test test/appshell-import-boundary.test.mjs` passed after removing stale admin, bot, page-helper, and settings-dialog imports from `src/AppShell.tsx`.
- Focused admin display helper test: `node --experimental-strip-types --test test/admin-display.test.mjs` passed after moving byte labels, collector status/purpose text, scheduled-job progress text, and Discord audit labels out of `src/components/admin/AdminPanel.tsx`.
- Focused market analytics test: `node --experimental-strip-types --test test/market-analytics.test.mjs` passed after extracting best-seller sort helpers into the market analytics module.
- Focused market listing helper test: `node --experimental-strip-types --test test/market-listing-utils.test.mjs` passed after extracting market listing display, date, age, JSON, and tracking-key helpers.
- Focused Market page shared-browser-helper test: `node --experimental-strip-types --test test/market-page-shared-browser-utils.test.mjs` passed after keeping Market imports wired to shared analytics and navigation helpers.
- Focused Market page boundary test: `node --experimental-strip-types --test test/market-page-boundary.test.mjs` passed after moving the remaining routed Market component and tools out of `src/pages/MainPages.tsx`, importing Market directly from `src/AppShell.tsx`, and removing the legacy file.
- Focused Price Finder boundary test: `node --experimental-strip-types --test test/price-finder-boundary.test.mjs` passed after moving the price finder out of `src/pages/MarketPage.tsx` and into `src/pages/market/PriceFinder.tsx`.
- Focused Buy Order Finder boundary test: `node --experimental-strip-types --test test/buy-order-finder-boundary.test.mjs` passed after moving the buy-order finder out of `src/pages/MarketPage.tsx` and into `src/pages/market/BuyOrderFinder.tsx`.
- Focused Dashboard page boundary test: `node --experimental-strip-types --test test/dashboard-page-boundary.test.mjs` passed after moving the routed Dashboard component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused Leaderboard page boundary test: `node --experimental-strip-types --test test/leaderboard-page-boundary.test.mjs` passed after moving the routed Leaderboard component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused Activity page boundary test: `node --experimental-strip-types --test test/activity-page-boundary.test.mjs` passed after moving the routed Activity component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused Inventory page boundary test: `node --experimental-strip-types --test test/inventory-page-boundary.test.mjs` passed after moving the routed Inventory component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused Map page boundary test: `node --experimental-strip-types --test test/map-page-boundary.test.mjs` passed after moving the routed Map component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused Public Craft Finder page boundary test: `node --experimental-strip-types --test test/public-craft-finder-page-boundary.test.mjs` passed after moving the routed Public Craft Finder component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused Production page boundary test: `node --experimental-strip-types --test test/production-page-boundary.test.mjs` passed after moving the routed Production component out of `src/pages/MainPages.tsx` and importing it directly from `src/AppShell.tsx`.
- Focused active-region boundary test: `node --experimental-strip-types --test test/active-regions-boundary.test.mjs` passed after moving shared active-region fetch and label helpers out of `src/pages/MainPages.tsx` and into `src/hooks/useActiveRegions.ts`.
- Focused server route/helper test: `node --experimental-strip-types --test test/server-route-groups.test.mjs` passed after the route classification, visitor logging, security header, MIME type, and static cache policy extraction.
- Focused server response helper test: `node --experimental-strip-types --test test/server-http-responses.test.mjs` passed after extracting JSON, text, and binary response writers.
- Focused server cookie helper test: `node --experimental-strip-types --test test/server-http-cookies.test.mjs` passed after extracting cookie parsing and HttpOnly session-cookie serialization.
- Focused server request helper test: `node --experimental-strip-types --test test/server-http-requests.test.mjs` passed after extracting origin, same-origin, and safe return-path helpers.
- Focused server CSRF helper test: `node --experimental-strip-types --test test/server-http-csrf.test.mjs` passed after extracting admin CSRF token derivation and exact-match header validation.
- Focused server body helper test: `node --experimental-strip-types --test test/server-http-bodies.test.mjs` passed after extracting body-size limits, streamed raw-body reading, JSON parsing, and body-too-large errors.
- Focused server rate-limit helper test: `node --experimental-strip-types --test test/server-http-rate-limit.test.mjs` passed after extracting request-address handling, route policies, bucket windows, and 429 retry responses.
- Focused server visitor IP helper test: `node --experimental-strip-types --test test/server-visitor-ip.test.mjs` passed after extracting IP normalization, anonymization, and salted hash helpers.
- Focused server notification-activity helper test: `node --experimental-strip-types --test test/server-notification-activity.test.mjs` passed after extracting recursive public notification metadata redaction.
- Focused server deal-alert helper test: `node --experimental-strip-types --test test/server-deal-alerts.test.mjs` passed after extracting public deal-alert row shaping and Discord DM payload formatting.
- Focused server market-activity helper test: `node --experimental-strip-types --test test/server-market-activity.test.mjs` passed after extracting market listing normalization, timestamp coercion, trade/listing matching, and event source-key helpers.
- Focused server production-activity helper test: `node --experimental-strip-types --test test/server-production-activity.test.mjs` passed after extracting production craft stable identity, metrics, display, and profession-key helpers.
- Focused server recipe-catalog helper test: `node --experimental-strip-types --test test/server-recipe-catalog.test.mjs` passed after extracting recipe catalog keys, kind parsing, and target normalization.
- Focused server Discord settings helper test: `node --experimental-strip-types apps/bitcraft-local/test/server-discord-settings.test.mjs` passed after extracting Discord settings defaults, role-panel, welcome-flow, presence, notification-channel, and colour-role normalization.
- Focused server visitor security settings helper test: `node --experimental-strip-types apps/bitcraft-local/test/server-visitor-security-settings.test.mjs` passed after extracting visitor-security retention, GeoIP provider/cache, account, and license-key setting normalization.
- Focused server market deal-watch settings helper test: `node --experimental-strip-types apps/bitcraft-local/test/server-market-deal-watch-settings.test.mjs` passed after extracting market deal-watch settings normalization.
- Focused server collector settings helper test: `node --experimental-strip-types apps/bitcraft-local/test/server-collector-settings.test.mjs` passed after extracting collector defaults, payload-domain ownership maps, current-table ownership maps, and dashboard collector setting normalization.
- Focused server scheduled-jobs helper test: `node --experimental-strip-types --test test/server-scheduled-jobs.test.mjs` passed with 8 tests after extracting schedule parsing, serialization, next-run calculation, labels, scheduled-job registry seeding/repair, stale-run recovery, and status shaping behavior.
- Focused CSS ownership test: `node --experimental-strip-types apps/bitcraft-local/test/css-ownership.test.mjs` passed after moving global mobile nav scrollbar rules out of `src/styles/setup-workflow.css`, extracting routed Dashboard selectors into `src/styles/dashboard.css`, extracting routed Leaderboard page selectors into `src/styles/leaderboard.css`, extracting routed Production page selectors, private-craft toggle, and crafter filter row into `src/styles/production.css`, extracting routed Public Craft Finder selectors and map-location link controls into `src/styles/public-craft.css`, renaming shared command/filter primitives to `command-filter-*`, extracting routed Market selectors and deal-watch controls into `src/styles/market.css`, extracting routed Craft Calculator selectors into `src/styles/craftcalc.css`, extracting routed Members selectors into `src/styles/members.css`, extracting routed Skills selectors into `src/styles/skills.css`, extracting routed Inventory selectors into `src/styles/inventory.css`, extracting routed Construction selectors into `src/styles/construction.css`, extracting routed Research selectors into `src/styles/research.css`, extracting routed Activity selectors into `src/styles/activity.css`, extracting routed Region selectors into `src/styles/region.css`, extracting routed Sync selectors into `src/styles/sync.css`, extracting routed Map selectors into `src/styles/map.css`, extracting shared admin console, database, user, audit, backup, and maintenance selectors into `src/styles/admin.css`, extracting Discord/bot admin section selectors into `src/styles/discord-admin.css`, and extracting standalone `/bot` dashboard shell selectors into `src/styles/bot-dashboard.css`.
- Focused server BitJita proxy-cache helper test: `node --experimental-strip-types --test test/server-bitjita-proxy-cache.test.mjs` passed with 3 tests after extracting proxy TTL policy, request headers, in-flight dedupe, stale-if-error fallback, and cache pruning into `src/server/bitjitaProxyCache.mjs`.
- Focused server admin-permissions helper test: `node --experimental-strip-types --test test/server-admin-permissions.test.mjs` passed with 3 tests after extracting admin role labels, safe role fallback, scoped permissions, owner wildcard behavior, and route permission mapping into `src/server/adminPermissions.mjs`.
- Focused server auth-identity helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-auth-identity.test.mjs` passed with 3 tests after extracting admin username policy, Discord ID policy, and Discord profile display-name fallback into `src/server/authIdentity.mjs`.
- Focused server sessions helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-sessions.test.mjs` passed with 3 tests after extracting admin/app session cookie names, lifetimes, token hashing, session payload creation, request token extraction, and clear-cookie behavior into `src/server/serverSessions.mjs`.
- Focused server session-lookup helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-session-lookups.test.mjs apps/bitcraft-local/test/server-sessions.test.mjs` passed with 6 tests after extracting DB-backed admin/app session lookup into `src/server/sessionLookups.mjs` while preserving the existing session cookie policy in `src/server/serverSessions.mjs`.
- Focused server Discord OAuth config helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-discord-oauth-config.test.mjs` passed with 3 tests after extracting Discord OAuth client ID, client secret, redirect URI, and enabled-state resolution into `src/server/discordOAuthConfig.mjs`.
- Focused server Discord OAuth flow helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-discord-oauth-flow.test.mjs` passed with 8 tests after extracting Discord authorize URL construction, callback redirect decisions, safe return-path fallback, matching-state acceptance, token/profile request descriptors, token exchange form fields, profile account mapping, and success redirect cookie ordering into `src/server/discordOAuthFlow.mjs`.
- Focused server OAuth-state helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-oauth-state.test.mjs` passed with 5 tests after extracting Discord OAuth state secret reuse/generation, cookie policy, signing, tamper rejection, return-path clamping, signed-cookie reading, and clear-cookie behavior into `src/server/oauthState.mjs`.
- Focused server default-owner-admin helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-default-owner-admin.test.mjs` passed with 5 tests after extracting default owner Discord admin seeding into `src/server/defaultOwnerAdmin.mjs`.
- Focused server prepared-statements helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-prepared-statements.test.mjs` passed with 1 test after extracting SQLite prepared statement creation into `src/server/preparedStatements.mjs`.
- Focused server schema-bootstrap helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-schema-bootstrap.test.mjs` passed with 2 tests after extracting the SQLite schema bootstrap SQL and application helper into `src/server/schemaBootstrap.mjs`.
- Focused server schema-migrations helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-schema-migrations.test.mjs` passed with 5 tests after extracting additive column migration metadata, legacy schema cleanup, release-sensitive bootstrap index statements, and migration/index application helpers into `src/server/schemaMigrations.mjs`.
- Focused server default-app-settings helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-default-app-settings.test.mjs` passed with 5 tests after extracting default app setting seed rows, JSON defaults, obsolete setting cleanup keys, and default insert/delete application into `src/server/defaultAppSettings.mjs`.
- Focused server app-release helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-app-release.test.mjs` passed with 4 tests after extracting release environment revision preference, git ref/detached-HEAD fallback, safe missing-git fallback, and app version/build release-key formatting into `src/server/appRelease.mjs`.
- Focused server app-settings-policy helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-app-settings-policy.test.mjs` passed with 6 tests after extracting claim ID, saved/submitted refresh interval, saved/submitted retention day, region ID, stored/submitted excluded-member normalization, app default-page allow-list, and BitCraft Sync URL validation into `src/server/appSettingsPolicy.mjs`.
- Focused server password-auth helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-password-auth.test.mjs` passed with 3 tests after extracting legacy password minimum length, scrypt password hashing, matching-password verification, wrong-password rejection, and malformed stored password rejection into `src/server/passwordAuth.mjs`.
- Focused server admin-login-attempts helper test: `node --experimental-strip-types --test apps/bitcraft-local/test/server-admin-login-attempts.test.mjs` passed with 3 tests after extracting admin login-attempt keying, five-failure limit, fifteen-minute expiry, expired failure reset, and success clearing into `src/server/adminLoginAttempts.mjs`.
- Focused server integration test: `node --experimental-strip-types --test test/server.test.mjs` passed after the admin-role permission fix, route-helper import wiring, market-activity helper import wiring, production-activity helper import wiring, recipe-catalog helper import wiring, scheduled-job helper import wiring, BitJita proxy-cache import wiring, admin-permissions/public-users/admin-request-guards/auth-identity/session-policy/session-lookup/discord-oauth-config/oauth-state/app-settings-policy/password-auth/admin-login-attempt import wiring, GeoIP license-key redaction check, Discord bot-token redaction check, and notification-activity metadata redaction check.
- Full production build: `corepack pnpm --filter @workspace/bitcraft-local run build` passed after extracting the BitJita proxy-cache, admin-permissions, public-users, admin-request-guards, auth-identity, session-policy, session-lookup, Discord-OAuth-config, Discord-OAuth-flow, OAuth-state, schema-bootstrap, prepared-statements, default-owner-admin, schema-migrations, default-app-settings, scheduled-jobs, app-release, app-settings-policy, password-auth, and admin-login-attempt helpers and updating the latest server-boundary documentation.
- Full app test suite: `corepack pnpm --filter @workspace/bitcraft-local test` passed with 250 tests after extracting the BitJita proxy-cache, admin-permissions, public-users, admin-request-guards, auth-identity, session-policy, session-lookup, Discord-OAuth-config, Discord-OAuth-flow, OAuth-state, schema-bootstrap, prepared-statements, default-owner-admin, schema-migrations, default-app-settings, scheduled-jobs, app-release, app-settings-policy, password-auth, and admin-login-attempt helpers, adding focused proxy-cache/admin-permission/public-user/admin-request-guard/auth-identity/session-policy/session-lookup/discord-oauth-config/discord-oauth-flow/oauth-state/schema-bootstrap/prepared-statements/default-owner-admin/schema-migrations/default-app-settings/scheduled-jobs/app-release/app-settings-policy/password-auth/admin-login-attempt coverage plus the `/bot` accepted notification exception doc sync, signed-in deal-alert dedupe scoping, and shared table-sort, generic sort-button, app-chrome, and Public Craft Finder action click-target CSS regression coverage, preserving proxy/admin/auth payload, mutation-guard integration behavior, notification user privacy, and the latest server-boundary documentation.
- `git diff --check` passed for this slice, with only Windows CRLF warnings from Git.
- Targeted secret/config checks were repeated on 2026-06-29 against the current worktree after the schema/default-setting/default-owner/release-metadata, notification, CSS, and README updates: tracked sensitive filename checks found only the committed `.env.example` template and no tracked runtime databases, logs, private keys, certificate bundles, backups, uploads, `.codex-dev/`, or `outputs/`; local generated `.codex-dev` smoke logs plus `.dev-data` and `.test-data-*` SQLite files were present on disk but verified ignored by existing `.gitignore` rules; token/private-key-shaped scans found no committed Discord/GitHub/OpenAI/Slack/AWS/Google/private-key shaped secrets; a name-based scan found only `.env.example`/deployment placeholders, expected server secret plumbing, documentation, and test-only fake secret sentinel values.
- Deployment docs were rechecked on 2026-06-29 after the schema/default-setting/default-owner/release-metadata helper extractions against the current package engine/version metadata, root `pnpm@11.1.3` policy, server environment defaults, `.env.example`, systemd service, and Caddy example; no deployment-doc corrections were required in that pass.
- Browser smoke notification probe verified market listing and market sale toasts plus drawer persistence across 16 main app pages and Admin; `/bot` was verified as a documented exception because it does not mount `DashboardApp`. Later built-app smoke checks loaded Dashboard, Activity, Market, and Production with notification chrome present and no browser console errors after the AppShell notification-source extraction, verified the split notification stylesheet still applies drawer overlay, drawer panel, notification button, and toast-stack styles, verified the extracted user-settings stylesheet still applies the settings dialog, preferences sound controls, and expanded theme editor styles, and verified the AppShell toast-setting normalization wiring renders Preferences with sane default sound controls and no console errors. A 2026-06-28 CSS/UX smoke checked Dashboard, Market, Inventory, Empires, Admin, and `/bot` at desktop size plus Dashboard, Market, Empires, and `/bot` at 390x844; all rendered nonblank content, reported no browser console errors, had no root-level horizontal overflow, and table-heavy pages kept overflow inside scroll containers. A 2026-06-28 follow-up inserted smoke-only market listing rows, verified Dashboard toast creation, unread badge clearing, drawer close, drawer-to-Market navigation, drawer persistence on Production with market and production entries, and user-level listing settings blocking a smoke listing while disabled. Controlled dismissal probes then verified refresh-triggered market listing toast timing, confirmed `elementFromPoint` over the dismiss button targets the toast after the z-index fix, confirmed manual dismissal removes visible toasts, confirmed the drawer still retains dismissed notices, and reported no browser console errors on Dashboard, Market, Activity, Map, Production, and Admin. A 2026-06-28 Empires smoke check inserted `codex-empires-smoke-1782672570618`, loaded `/?page=empires`, used the real Refresh button, verified the market-listing toast, unread badge, drawer entry, drawer-to-Market navigation, and no browser console errors. A built-app app-chrome smoke check after extracting `src/styles/app-chrome.css` verified the Dashboard floating action rail and Help dialog at desktop size, the keyboard command palette shell, the dedicated Terms page legal document styles, and the mobile floating-action offset at 390px width; browser console errors were empty. The cookie banner was not visible in that browser session because analytics consent had already been stored, so cookie-banner visual verification remains a separate manual/browser check if consent state can be reset safely. A later browser sound smoke check used the real Preferences UI on Dashboard, Production, and Market, confirmed the Preview button, sound-enabled checkbox, sound selector, and volume control were visible, clicked Preview on each page, and captured no browser console errors. A later loopback smoke bridge pass used `smokeNotification` query triggers to verify all five supported notification types on all 17 routed main-app pages, totaling 85/85 visible toast checks with no failures, no retained smoke query parameters, and no browser console errors; it also verified visible dismissal and drawer persistence for production-started and market-deal-alert notices. A 2026-06-29 built-app smoke after extracting `src/notifications/useBrowserNotificationSmoke.ts` loaded Dashboard with `smokeNotification=market-deal-alert`, observed the visible market-deal toast and unread badge, confirmed the smoke query parameters were removed from the URL, and captured no browser console errors. After extracting `src/notifications/useBrowserNotificationSources.ts`, another Dashboard smoke with `smokeNotification=production-started` confirmed the app mounted, displayed a production toast plus unread badge, removed smoke query parameters, had no root-level overflow, and captured no browser console errors. After extracting `src/notifications/useToastNotifications.ts`, a follow-up Dashboard smoke with `smokeNotification=market-sale` confirmed the app mounted, displayed a market-sale toast plus unread badge, removed smoke query parameters, had no root-level overflow, and captured no browser console errors. A later 2026-06-29 structured browser audit used the already-running smoke server without restarting it, checked Dashboard, Members, Professions, Production, Inventory, Construction, Research, Market, Region, Map, Sync, Activity, Admin, Public Craft Finder, Craft Calculator, and `/bot` at 1280x720 and 390x844, and found nonblank main content, no root-level horizontal overflow, no captured browser console errors, notification chrome on all main-app pages, and no notification chrome on `/bot` as expected. That audit found a narrow 21px Region `#` sort button in a mobile-width/fine-pointer viewport; `styles.css` now gives shared `.sort-button` controls a 28px minimum width, and `test/css-ownership.test.mjs` asserts that regression guard.

Run the full app build and full test suite again before the next push or final release claim if additional code changes are made.

## Remaining Release Blockers

The release-readiness goal is not complete until every row below has current evidence. Treat the `Evidence needed to close` column as the release completion checklist.

| Area | Current evidence | Evidence needed to close |
| --- | --- | --- |
| Browser notification page/type matrix | Loopback smoke verification covered all five supported browser notification types on Dashboard, Leaderboard, Members, Professions/Skills, Production, Inventory/Storage, Construction, Research, Market, Region, Empires, Map, Sync, Activity, Admin, Public Craft Finder, and Craft Calculator. Dismissal, drawer persistence, badge clearing, drawer navigation, user setting gates, market-listing dismissal, production/deal-alert dismissal, and representative sound Preview checks have evidence. | Keep the existing evidence unless notification UI, routing, settings, source queueing, sound settings, or supported notification types change again. If they change, repeat the affected browser checks and update this audit with dated evidence. |
| Live-source notification generation | `src/notifications/verificationMatrix.ts` and `test/notification-matrix.test.mjs` keep the blocker list explicit: `market-deal-alert`, `production-started`, and `production-completed` remain `required`; market listing and market sale are `verified` with dated `liveEvidence` references. `docs/notification-live-source-verification.md` defines the runbook. | Capture dated evidence that real production queue diffs emit `Craft started` and `Craft completed` toasts through the globally mounted source hook, and that a signed-in Discord-linked user's real deal-alert feed emits a `Market deal found` toast without leaking private alerts to another user. Then update the matrix status, add dated `liveEvidence` references, and update tests so `liveSourceNotificationVerificationComplete()` returns `true`. |
| Dedicated `/bot` notification behavior | Closed for this release under `accepted-intentional-exception`: `/bot` is documented, tested in `verificationMatrix.ts`, and smoke-verified as an intentional exception because it mounts `BotControlApp` without the main app notification chrome. | No further release action unless `/bot` is redesigned to share the main app chrome; if that happens, add it to the page/type matrix and verify it like other routed pages. |
| CSS and UX audit | Page-owned styles, app chrome, notification, admin, Discord/bot, and bot dashboard styles have been split into focused modules with ownership tests. Representative desktop/mobile smoke checks found and fixed several overflow/clickability issues; the 2026-06-29 built-app passes also fixed undersized shared table sort, generic sort, app-chrome, and Public Craft Finder action click targets, verified the populated Market/Public Craft tables after CSS changes, verified Admin plus `/bot` desktop/mobile access-state surfaces, and structurally audited the main routed pages for nonblank content, root overflow, notification chrome presence, and browser console errors. | Complete deeper screenshot review for key dashboard/admin/bot pages and important states that are not covered by existing smoke evidence, especially populated/empty/error/loading/stale states. Record concrete fixes or explicit keep-as-is decisions for any unused, duplicate, fragile, or broad selectors found later. |
| Server architecture | Current release-readiness pass has a documented split map and focused tests for the stable dependency-light server helpers: route policy, response/cookie/request helpers, CSRF, bodies, rate limits, visitor privacy/security, notification redaction, deal alerts, market/production/recipe/Discord/collector/scheduled-job schedule/bootstrap/recovery/status policy, BitJita proxy cache, admin permissions, public users, mutation guards, auth identity, session policy, DB-backed session lookup policy, Discord OAuth config/flow/state policy, schema bootstrap SQL, prepared-statement factory, default owner admin bootstrap, schema migration metadata/application/cleanup, default app settings bootstrap/application, release metadata, app settings, password-auth, and admin login-attempt policy. This is sufficient for the current release-maintainability pass without moving the request dispatcher. | Future route/service splits remain optional follow-up work unless new behavior changes require them. If continued later, move Discord OAuth network/database/session side effects, larger database lifecycle setup, scheduled jobs/collectors, Discord services, and route groups only through dependency-injected modules with fresh focused and integration tests after each move. |
| Security/config/deployment hygiene | Secret/config and deployment-doc checks have dated 2026-06-29 evidence after the latest schema/default-setting/default-owner/release-metadata, notification, CSS, and README changes; generated local smoke/test databases and logs were present only under ignored paths. Server tests cover Discord bot-token redaction, GeoIP license-key redaction, notification metadata redaction, admin roles, CSRF, and auth/session integration paths. | Repeat targeted secret scan and deployment-doc review after any further deployment, Discord, auth, settings, environment-file, storage-path, port, or release metadata changes. Record the dated result before final release. |
| Final automated gate | Current 2026-06-29 worktree evidence: `corepack pnpm --filter @workspace/bitcraft-local run build` passed, including `tsc -p tsconfig.json --noEmit` and Vite production build; `corepack pnpm --filter @workspace/bitcraft-local test` passed with 250 tests, 250 passing, 0 failing, 0 skipped. Focused notification source tests passed with 20 tests in the same release slice. The app has no configured `lint` script; TypeScript checking is covered by the app build command. | Run the full production build and full app test suite again after any further code changes that will be included in the release. Record exact commands, pass counts, and any skipped checks, including the absence of a configured lint script unless one is added before release. |
